import styled from 'styled-components';

export const Container = styled.div`
  background: var(--black);
  height: 5rem;
  display: flex;
  align-items: center;
  padding: 0 2rem;

  h1 {
    color: var(--white);
    font-size: 1.375rem;
  }
`;
